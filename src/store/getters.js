const getters = {
  menu: state => state.app.menu,
  products: state => state.app.products,
  inquire: state => state.inquire.inquire,
  basket: state => state.inquire.basket
};
export default getters;
